[package]
name = "perl-value"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Perl value types: Scalar, Value, Arc-based shared types"

[dependencies]
perl-string = { path = "../perl-string" }
